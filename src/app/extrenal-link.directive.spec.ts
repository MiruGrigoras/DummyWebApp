import { ExtrenalLinkDirective } from './extrenal-link.directive';

describe('ExtrenalLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtrenalLinkDirective();
    expect(directive).toBeTruthy();
  });
});
